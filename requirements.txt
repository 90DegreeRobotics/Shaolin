# Chirox runtime dependencies (versions verified present on the build machine).
# Sovereign stack: no third-party cloud services. The Master's voice runs on a
# local Ollama model; the reflex layer is local computer vision.

# mediapipe 0.10.31 is a Tasks-only build (no legacy `mp.solutions`). Chirox uses
# the Tasks PoseLandmarker; the ~9MB pose_landmarker.task model is downloaded once
# on first use into Dojo/models/ (see chirox/vision/tracker.py).
mediapipe==0.10.31
opencv-python==4.12.0.88
numpy==2.4.2
requests==2.32.4

# Development / verification
pytest==9.0.3
