# Chirox Build Witness — 2026-07-01

Continues `PROOF_2026-06-30.md`. Records what was actually run and inspected today.
No personal Dojo Record data.

## Honest correction (Truthful Operation)

Two things from prior sessions were corrected, in the open:

1. **Start date.** The ledger start was set to 2026-06-28 by my own estimate; the
   practitioner clarified today is **day 3**, so day 1 is **2026-06-29**. The
   correction was sealed as a `practice_start_set` event (previous → new), not
   silently overwritten. `chirox today` now reads Day 3.
2. **MediaPipe API.** The vision pipeline (and the original prototype) used the
   legacy `mediapipe.solutions.pose` API. The installed **mediapipe 0.10.31 is a
   Tasks-only build** — `mp.solutions` does not exist — so that path could never
   have run here. Earlier STATUS framed the live run as "hardware-gated"; the fuller
   truth is it was also API-broken, discovered on first execution. Migrated to the
   **Tasks API** (`PoseLandmarker`); same 33-landmark topology, geometry unchanged.

## Tests

`python -m pytest` → **64 passed** (added: calendar quarter, Diet lane, recorder,
tracker). Vision geometry, codex/Sentinel, ingest, curriculum, discernment, and
recorder logic all covered.

## Vision — verified on a real human (not just plumbing)

- Downloaded `pose_landmarker.task` (full, 9.4 MB) to `Dojo/models/` (git-ignored).
- Tasks `PoseLandmarker` created + ran; on a real person image it detected **1 pose,
  33 landmarks**, fed through the actual Chirox geometry:
  - `general_joint_angles` → real degrees (L-knee 175.7, R-knee 111.7, elbows ~170).
  - `evaluate_horse_stance` → deterministic verdict "too high / slouching" at
    **confidence 0.988** (correct for a standing figure). Full path works.
- `chirox record` ran end-to-end on a synthetic clip: 30 frames processed, an mp4
  written to the archive, and an honest manifest ("No body detected — only raw video
  saved") — the record→archive→manifest loop works without a camera.

## Recorder / timeline

`chirox record --exercise <name>` archives video to git-ignored `Dojo/media/<exercise>/`,
measures raw range-of-motion (never a form grade), and seals a `session_recording`
manifest into the Codex. `chirox timeline` lists sealed recordings oldest-first — the
visual improvement index. Manifest sealing + ordering are unit-tested; real video
writing is proven by `test_video_writer_produces_a_real_file`.

## Still honestly gated

- **Live webcam session** by the practitioner (same code path; needs a person + camera).
- **Synchronized multi-camera capture on the physical Weatherman rig** (needs hardware).

## Wisdom, sage, voice, unified identity (same day, later)

- **Wisdom corpus.** Fetched public-domain texts (Tao Te Ching/Legge, Analects/Legge,
  Dhammapada/Müller, Art of War/Giles). `WisdomLibrary` parsed **1654 passages**; search
  returned the exact right lines (TTC ch. 8 "the highest excellence is like water").
- **Sage (live, 14B).** `chirox sage` posed a question grounded in and citing *The Art of
  War, Ch. III*, reflected honestly on the answer (named what it revealed and avoided, one
  thing to sit with — no numeric grade), tagged growth `searching` + themes, and sealed a
  `wisdom_dialogue` into the ledger (seq 4). `chirox growth` tallies themes/engagement.
- **Voice (local, sovereign).** Piper TTS + faster-whisper STT. TTS→STT round-trip:
  spoken "The first victory is showing up tomorrow." → transcribed identically (**100%**),
  fully offline. `chirox say` synthesizes a wav; `--speak`/`--listen` wired into debrief/sage.
- **One identity.** Reconciled the docs (Chirox_Integration_Plan, STATUS, ROADMAP, README):
  only **Master Chirox** is surfaced; internal engines are never separate voices; Chronos is
  named only as an external pattern source, not a runtime dependency.
- **Tests:** 73 passing.

