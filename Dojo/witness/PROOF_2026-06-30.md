# Chirox Build Witness — 2026-06-30

This file records what was actually run and inspected, per the project's proof
standard ("Do not say a feature works until it has been run and its output
inspected"). It contains no personal Dojo Record data.

## Test suite

`python -m pytest` → **49 passed**. Coverage:

- `test_calendar.py` — day/phase/week/checkpoint/weekly-review math (8).
- `test_codex.py` — hash-chain linkage, tamper detection, recorded (not silent)
  forgetting, and the Sentinel gate: provisioning, sealed decisions, fail-closed
  denial without an operator, shadow mode, single-use grants (9).
- `test_ingest.py` — template parsing, required-field enforcement, pain-range
  rejection, and commit-through-Sentinel ordering (7).
- `test_curriculum.py` — manual parsing + deterministic topic retrieval (6).
- `test_stances.py` — pure stance geometry, the UNCERTAIN gate, session
  aggregation, and multi-camera fusion (10).
- `test_discernment.py` — green/red/overload signals, hardass persona invariants,
  evidence assembly, and honest refusal when the model is absent (9).

## Deterministic reflex

`sample_vision_session.json` in this folder is a real emitted session summary
from synthetic landmark input (no camera, no personal data): 86 frames, 6 flagged
UNCERTAIN and **excluded from the verdict**, a `stance_too_high` drift caught 20
times, `time_in_tolerance_s` computed. This is the machine measuring and reporting
with explicit uncertainty — never asserting form on thin evidence.

## Codex + Sentinel (live CLI run)

`chirox init` → `chirox log daily` sealed a three-event chain, verified by
`chirox verify` ("Codex intact: 3 sealed events, chain verified"):

```
seq 0  operator_provisioned          prev_hash 000…000
seq 1  sentinel_authority_decision   prev_hash <hash of seq 0>
seq 2  daily_checkin                 prev_hash <hash of seq 1>
```

The authorization decision is sealed **before** the record — Sentinel Law and
Forever Law both demonstrated.

## Master voice (live, local Ollama)

`ollama serve` + `chirox debrief` produced a real debrief from `qwen2.5:7b-instruct`
over a seeded day. Verified in the output: the Master **refused to assess form**
("No vision session recorded, so I cannot assert anything about your form"), cited
the manual phase section, read the forge signals and drove through them, and ended
with a non-negotiable minimum. No fabrication; when pointed at a dead port the
Master reports unavailability instead of inventing (test `test_master_refuses_
honestly_when_model_absent`).

## Honestly gated (not proven here)

- **Live single-camera stance run** needs a person in front of the webcam. The
  geometry is proven by unit tests; the live capture is an integration step.
- **Synchronized multi-camera capture on the physical Weatherman rig** needs the
  hardware (two C920s into OBS). The fusion logic is built and single-source
  verified; the two-camera live run is pending hardware.
